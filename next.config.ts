import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Image optimization for child-friendly assets
  images: {
    domains: ['localhost'],
    formats: ['image/webp', 'image/avif'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },
  
  // Child-safe configuration
  experimental: {
    // Reserved for future experimental features
  },
  
  typescript: {
    // Type check during builds
    ignoreBuildErrors: false,
  },
  
  // Performance optimizations for child attention spans
  poweredByHeader: false,
  compress: true,
  
  // Security headers for child safety
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Frame-Options',
            value: 'DENY',
          },
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'Referrer-Policy',
            value: 'strict-origin-when-cross-origin',
          },
          {
            key: 'X-DNS-Prefetch-Control',
            value: 'on',
          },
        ],
      },
    ];
  },
};

export default nextConfig;